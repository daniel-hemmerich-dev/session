# A simple session service
This is a simple session service written in Java with Spring Boot and MongoDB.

## Setup
1. Set your api key and database credentials in the `src/main/resources/application.properties` file
2. Set your api key also in the `cron.yaml` file.